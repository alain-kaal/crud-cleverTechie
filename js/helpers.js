function resetForm() {
  // Get the first form with the name
  // Hopefully there is only one, but there are more, select the correct index
  
  form.reset();
  console.log("Form has been gesendet ...");
    // evt.preventDefault();
  // Prevent page refresh
  return false; 
}